# PostgreSQL (공통 메타 DB)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=cloud_ledger

# AWS (서비스 자체 계정 — 테넌트 AssumeRole 호출용)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_ROLE_SESSION_NAME=cloud-ledger-session
